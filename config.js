// Default values applied when the page first loads.
// Edit this file to change the initial state of the simulator.

export const DEFAULTS = {
  // sliders, all 0..1
  degeneration: 0.55,   // degeneration of central field
  netDensity: 0.40,     // density of flashing net
  transparency: 0.75,   // periphery transparency

  // 'camera' | 'video'
  background: 'camera',

  // 'immersive' | 'sideBySide'
  view: 'immersive',

  // start with the menu collapsed to the hamburger button?
  menuCollapsed: false,
};
